spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/my_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect  # добавляем явный диалект
    show-sql: true